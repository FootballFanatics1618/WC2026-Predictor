{
  "name": "wc2026-predictor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start",
    "test": "npx playwright test",
    "test:api": "npx playwright test tests/api/",
    "test:e2e": "npx playwright test tests/e2e/",
    "test:setup": "node -e \"require('./tests/setup').saveAllOriginals().then(() => console.log('Originals saved'))\"",
    "test:teardown": "node -e \"require('./tests/setup').restoreAll().then(() => console.log('All restored'))\""
  },
  "dependencies": {
    "@supabase/auth-helpers-nextjs": "0.10.0",
    "@supabase/supabase-js": "2.43.4",
    "date-fns": "3.6.0",
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "eslint": "8.57.0",
    "eslint-config-next": "^15.0.0"
  }
}
